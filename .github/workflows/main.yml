name: Node lint
On: [push,workflows_dispatch]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Install dependencies
      run: npm install
    - name: Test
      run: npm test
    - name: lint
      run: npm lint
